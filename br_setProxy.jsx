var curFolder = app.project.activeItem;var folderItems = curFolder.items;var regPatToReplace = /(dpx)/ig;var newReg ="jpg";for(i=1; i<= folderItems.length; i++){    setProxy(folderItems[i]);    }function setProxy(item){var mainFile = item.file;var mainStr = mainFile.toString();var newStr = mainStr.replace(regPatToReplace,"jpg");var proxyFile = new File(newStr);item.setProxyWithSequence(proxyFile,true);}